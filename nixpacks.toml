[phases.install]
cmds = ["dotnet restore backend/RenovoWorkshop.sln"]

[phases.build]
cmds = ["dotnet publish backend/RenovoWorkshop.Api/RenovoWorkshop.Api.csproj -c Release -o /app/out"]

[start]
cmd = "dotnet /app/out/RenovoWorkshop.Api.dll"
